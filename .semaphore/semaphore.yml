# This file is managed by ServiceBot plugin - Semaphore. The content in this file is created using a common
# template and configurations in service.yml.
# Any modifications made to ths file will be overwritten by the generated content in nightly runs.
# For more information, please refer to the page:
# https://confluentinc.atlassian.net/wiki/spaces/Foundations/pages/2871296194/Add+SemaphoreCI
version: v1.0
name: build-test-release
agent:
  machine:
    type: s1-prod-ubuntu24-04-amd64-1

fail_fast:
  cancel:
    when: "true"

execution_time_limit:
  hours: 4

queue:
  - when: "branch != 'master' and branch !~ '[0-9]+\\.[0-9]+\\.x' and branch !~ 'v[0-9]+\\.[0-9]+\\.[0-9]+\\-hotfix\\-x' and branch !~ 'v[0-9]+\\.[0-9]+\\.[0-9]+\\-spanner\\-hotfix\\-x'"
    processing: parallel

global_job_config:
  prologue:
    commands:
      - checkout
      - sem-version java 21
      - . cache-maven restore

blocks:
  - name: Release
    dependencies: []
    run:
      when: "branch != 'master' and branch !~ '[0-9]+\\.[0-9]+\\.x' and branch =~ 'v[0-9]+\\.[0-9]+\\.[0-9]+\\-spanner\\-hotfix\\-x' and (change_in('debezium-core/') or change_in('pom.xml') or change_in('.semaphore/'))"
    task:
      jobs:
        - name: Release debezium-core
          commands:
            - cp debezium-core/pom.xml debezium-core/pom.xml.orig
            - ci-tools ci-update-version
            - ci-tools ci-push-tag
            # Build debezium-core with original groupId (io.debezium) to keep dependencies intact
            - mvn -U -Dmaven.wagon.http.retryHandler.count=10 --batch-mode clean install -Passembly -pl debezium-core -am -DskipTests -DskipITs -Denforcer.skip=true
            # Extract version from the built JAR filename (most reliable method)
            - export DEBEZIUM_VERSION=$(ls debezium-core/target/debezium-core-*.jar | grep -v '\(sources\|javadoc\|tests\|shaded\)' | head -1 | sed 's|.*/debezium-core-\(.*\)\.jar|\1|')
            - echo "Deploying version - ${DEBEZIUM_VERSION}"
            # Verify version was extracted successfully
            - |
              if [ -z "$DEBEZIUM_VERSION" ]; then
                echo "ERROR: Could not determine version from built artifacts"
                ls -la debezium-core/target/
                exit 1
              fi
            # Deploy main JAR with io.confluent groupId, using original POM for correct transitive deps
            - mvn deploy:deploy-file
              -Durl=https://confluent-519856050701.d.codeartifact.us-west-2.amazonaws.com/maven/maven-snapshots/
              -DrepositoryId=confluent-codeartifact-internal
              -Dfile=debezium-core/target/debezium-core-${DEBEZIUM_VERSION}.jar
              -DpomFile=debezium-core/pom.xml.orig
              -DgroupId=io.confluent
              -DartifactId=debezium-core
              -Dversion=${DEBEZIUM_VERSION}
              -Dpackaging=jar
            # Deploy sources JAR
            - mvn deploy:deploy-file
              -Durl=https://confluent-519856050701.d.codeartifact.us-west-2.amazonaws.com/maven/maven-snapshots/
              -DrepositoryId=confluent-codeartifact-internal
              -Dfile=debezium-core/target/debezium-core-${DEBEZIUM_VERSION}-sources.jar
              -DgeneratePom=false
              -DgroupId=io.confluent
              -DartifactId=debezium-core
              -Dversion=${DEBEZIUM_VERSION}
              -Dpackaging=jar
              -Dclassifier=sources
            # Deploy javadoc JAR
            - mvn deploy:deploy-file
              -Durl=https://confluent-519856050701.d.codeartifact.us-west-2.amazonaws.com/maven/maven-snapshots/
              -DrepositoryId=confluent-codeartifact-internal
              -Dfile=debezium-core/target/debezium-core-${DEBEZIUM_VERSION}-javadoc.jar
              -DgeneratePom=false
              -DgroupId=io.confluent
              -DartifactId=debezium-core
              -Dversion=${DEBEZIUM_VERSION}
              -Dpackaging=jar
              -Dclassifier=javadoc
            # Deploy tests JAR
            - mvn deploy:deploy-file
              -Durl=https://confluent-519856050701.d.codeartifact.us-west-2.amazonaws.com/maven/maven-snapshots/
              -DrepositoryId=confluent-codeartifact-internal
              -Dfile=debezium-core/target/debezium-core-${DEBEZIUM_VERSION}-tests.jar
              -DgeneratePom=false
              -DgroupId=io.confluent
              -DartifactId=debezium-core
              -Dversion=${DEBEZIUM_VERSION}
              -Dpackaging=jar
              -Dclassifier=tests

after_pipeline:
  task:
    agent:
      machine:
        type: s1-prod-ubuntu24-04-arm64-0
    jobs:
      - name: Metrics
        commands:
          - emit-ci-metrics -p -a test-results
      - name: Publish Test Results
        commands:
          - test-results gen-pipeline-report
      - name: SonarQube
        commands:
          - checkout
          - sem-version java 21
          - emit-sonarqube-data -a test-results
