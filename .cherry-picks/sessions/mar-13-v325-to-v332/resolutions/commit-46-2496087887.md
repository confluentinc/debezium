# Resolution: Commit #46 - Publication Timeouts

| Field | Value |
|-------|-------|
| Source SHA | 2496087887 |
| Result SHA | 72dc946bd5 |
| Complexity | TRIVIAL |
| Conflict Files | `PostgresReplicationConnection.java` |

## Strategy

3 conflict regions in `PostgresReplicationConnection.java`. The source commit adds publication timeout handling: `SQL_LOCK_NOT_AVAILABLE` constant, `PUBLICATION_QUERY_FAILURE_MESSAGE` constant, and wraps two `stmt.execute()` calls with `setQueryTimeout()` + try-catch for `SQLException`.

The target already has ALL of this functionality:
- `SQL_LOCK_NOT_AVAILABLE` constant at line 66
- `PUBLICATION_QUERY_FAILURE_MESSAGE` constant at line 68 (slightly different wording: "Creation of publication failed" vs source's "Creation of publication slot failed")
- `executeWithTimeout(stmt, ...)` helper method that wraps `setQueryTimeout` + `execute` with proper timeout capture/restore
- try-catch blocks around both publication creation calls with the same `PSQLState.QUERY_CANCELED` and `SQL_LOCK_NOT_AVAILABLE` checks

**All 3 conflicts resolved by keeping target's code**, since target already implements the same feature via the more robust `executeWithTimeout` abstraction.

### Conflict 1 (line 68) — Message text
- Source: `"Creation of publication slot failed: ..."`
- Target: `"Creation of publication failed: ..."`
- Resolution: Kept target's wording (no "slot" — this is about publications, not slots)

### Conflict 2 (line 245) — Create publication timeout
- Source: `stmt.setQueryTimeout(toIntExact(...)); stmt.execute(createPublicationStmt);`
- Target: `executeWithTimeout(stmt, createPublicationStmt);`
- Resolution: Kept target's `executeWithTimeout` (same logic, better encapsulation)

### Conflict 3 (line 487) — Create/update filtered publication timeout
- Source: `stmt.setQueryTimeout(toIntExact(...)); stmt.execute(createOrUpdatePublicationStmt);`
- Target: `executeWithTimeout(stmt, createOrUpdatePublicationStmt);`
- Resolution: Kept target's `executeWithTimeout`

## Result Diff (conflicted file only)

No diff — the conflicted file `PostgresReplicationConnection.java` has zero net changes in the result commit. All source changes were already present in the target via `executeWithTimeout` helper. Only the 2 non-conflicting files (SqlServerConnectorIT.java, AbstractNotificationsIT.java) were modified by this commit.
