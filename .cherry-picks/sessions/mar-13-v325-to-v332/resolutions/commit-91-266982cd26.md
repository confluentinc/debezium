# Resolution: Commit #91 - Bump kotlin & commons package versions for CONMON CVE fix

| Field | Value |
|-------|-------|
| Source SHA | 266982cd26 |
| Result SHA | 281567dd8b |
| Complexity | TRIVIAL |
| Conflict Files | `debezium-connector-postgres/pom.xml`, `debezium-testing-testcontainers/pom.xml` |

## Strategy

2 conflict regions across 2 files. CVE fix commit adding kotlin-stdlib and commons-lang3 dependencies.

### debezium-connector-postgres/pom.xml (1 conflict, line 149)
- Source side has `mockito-core` dependency (pre-existing in source branch, NOT part of this commit's diff), `commons-lang3` dependency (NEW from this commit), and opening `<dependency>` tag for avro-converter
- Target has empty HEAD side (target doesn't explicitly declare mockito-core here — it's inherited)
- Resolution: Added only `commons-lang3` dependency (the commit's actual delta). Did NOT add mockito-core (pre-existing source context, not part of this commit).

### debezium-testing-testcontainers/pom.xml (1 conflict, line 3)
- Same recurring pattern — source has version 3.2.5.Final, different indentation; adds `<version.kotlin>1.6.0</version.kotlin>`
- Target has version 3.3.2.Final, proper indentation, `mockito.argLine` + `version.okio` properties
- Resolution: Kept target's version/indentation, added `<version.kotlin>1.6.0</version.kotlin>`

## Result Diff (conflicted files only)

### debezium-connector-postgres/pom.xml
```diff
+        <dependency>
+            <groupId>org.apache.commons</groupId>
+            <artifactId>commons-lang3</artifactId>
+            <version>${version.commons.lang3}</version>
+        </dependency>
+        <dependency>
             <groupId>io.confluent</groupId>
```

### debezium-testing-testcontainers/pom.xml
```diff
     <properties>
         <mockito.argLine>-javaagent:${org.mockito:mockito-core:jar}</mockito.argLine>
         <version.okio>3.4.0</version.okio>
+        <version.kotlin>1.6.0</version.kotlin>
     </properties>
```
